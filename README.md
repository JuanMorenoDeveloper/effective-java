# Java Effective Samples

## Item 69: Use exceptions only for exceptional conditions
* Evitar usar exceptions para controlar el flujo de la aplicación
* Preferir el uso estados para controlar el flujo del programa (ej. hasNext() en Iterator)
* Si se diseña un API (interface) no forzar a clientes a usar exceptions para controlar el flujo de la aplicación.

